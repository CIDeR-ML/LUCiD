# Archive

Standalone diagnostic scripts that were originally in `tests/` with `test_` prefixes
but contain zero `def test_*` functions — they are not pytest tests.

Moved here during the test restructuring to keep them available for reference
without cluttering pytest discovery.

To run any script: `python tests/archive/<script>.py`
