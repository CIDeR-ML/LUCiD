"""PhotonSim → SIREN data preparation utilities.

The `photon_lookup_table.h5` consumed by `lucid.siren.training.PhotonSimDataset`
is built from per-energy PhotonSim ROOT files by the two table builders in
this package. See `docs/SIREN_TRAINING_INPUTS.md` for the end-to-end pipeline.
"""
