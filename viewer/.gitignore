# Test/sample datasets — large HDF5 files produced by make_test_data.py or
# copied from production runs. Regenerate locally rather than committing.
sample_v3/
test_data/
__pycache__/

# Override parent's *.html exclusion — viewer's index.html is source.
!*.html
