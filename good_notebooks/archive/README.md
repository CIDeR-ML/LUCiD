# archive/ — superseded notebooks (reference only)

- **tracking_opt_development** — heuristic counts-loss 5-stage recon. Superseded by
  `../reconstruction/tracking_opt_development_likelihood` (likelihood loss) and the validated
  `lucid.fitting.fit_track_multistart`. Kept for the hierarchical grid-search reference.
